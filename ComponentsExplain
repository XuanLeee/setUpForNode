Components are key feature in angular, each component have there template.
This can splice whole project into reusable part.
Angular use component to build web page and using modules to package the component.
